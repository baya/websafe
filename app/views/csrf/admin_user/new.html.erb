<h2>CSRF-Victim-Site: Create Admin User </h2>

<p><span><%= current_admin.login %></span> <%= link_to '退出', csrf_logout_url, method: :delete%></p>
<h3>使用 GET 方法去请求创建 admin, 这是不安全的</h3>
<%= form_tag csrf_admin_user_unsafe_create_url, method: 'GET' do -%>
  <div>
    <label>帐号:</label>
    <%= text_field_tag 'admin[login]' %>
  </div>
  <div>
    <label>密码:</label>
    <%= password_field_tag 'admin[password]' %>
  </div>
  <%= submit_tag '创建' %>
<% end  -%>
